# Device Monitoring System - Краткая инструкция

1. Запуск проекта

```bash
# В корне проекта (где docker-compose.yml):
docker-compose up --build
```

После запуска:
- Frontend: http://localhost:4200
- Backend API: http://localhost:5000

---

2. Добавление тестовых данных

**Откройте новый терминал и выполните:**

### Добавить устройства:
```bash
# Устройство 1
curl -X POST http://localhost:5000/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"laptop-001","userName":"John","startTime":"2024-01-22T09:00:00Z","endTime":"2024-01-22T11:00:00Z","version":"1.0.0"}'

# Устройство 2  
curl -X POST http://localhost:5000/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"phone-002","userName":"Alice","startTime":"2024-01-22T14:00:00Z","endTime":"2024-01-22T15:30:00Z","version":"2.1.0"}'

# Еще сессия для первого устройства
curl -X POST http://localhost:5000/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"deviceId":"laptop-001","userName":"John","startTime":"2024-01-21T10:00:00Z","endTime":"2024-01-21T12:00:00Z","version":"1.0.0"}'
```

### Проверить что данные добавились:
```bash
# Все устройства
curl http://localhost:5000/api/devices

# Сессии конкретного устройства
curl http://localhost:5000/api/devices/laptop-001/sessions
```

---

3. Работа с интерфейсом

1. **Откройте:** http://localhost:4200
2. **Видите:** Список устройств (таблица)
3. **Кликните:** "View Sessions" на устройстве → увидите все его сессии
4. **Нажмите:** "← Back to Devices" чтобы вернуться
5. **Нажмите:** "Refresh" чтобы обновить данные

---

4. Команды Docker

```bash
# Основные команды
docker-compose up --build    # Собрать и запустить
docker-compose up -d         # Запуск в фоне
docker-compose down          # Остановить
docker-compose logs          # Показать логи
docker-compose ps            # Статус контейнеров

---

5. Быстрая проверка

1. **Запустить:** `docker-compose up --build`
2. **Добавить данные:** команды из раздела 2
3. **Открыть:** http://localhost:4200
4. **Проверить:** Видны устройства и можно кликать

**Готово!** 🚀